use test;
use sql;

DATABASE SimpleDB {
	TABLE test(
		id INTEGER PRIMARY KEY,
		name TEXT
	);
}

suite Simple {
	SQLite baseDb;
	SimpleDB c(baseDb);

	Str test = "variable";
	WITH c: INSERT INTO test VALUES (3, "test");
	WITH c: INSERT INTO test(id, name) VALUES (4, test);

	// for (row in WITH c: SELECT * FROM test) {
	// 	print("${row.id} - ${row.name}");
	// }

	test 1 == 0;
}
