use test;
use sql;

DATABASE SimpleDB {
	TABLE test(
		id INTEGER PRIMARY KEY,
		name TEXT
	);
}

class MyException extends sql:SQLError {
	init() {
		init("Test") {}
	}
}

suite Simple {
	SQLite baseDb;
	SimpleDB c(baseDb);

	test 1 == 0;
}
