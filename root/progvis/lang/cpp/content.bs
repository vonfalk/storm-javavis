class Content on Compiler {
	// Functions found.
	CppDecl[] functions;

	// Types found.
	CppType[] types;

	// Add functions.
	void add(CppDecl fn) {
		functions << fn;
	}

	// Add types.
	void add(CppType type) {
		types << type;
	}
}
