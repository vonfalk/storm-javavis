use ui;
use core:geometry;

class SimpleWin extends Frame {
	init() {
		init("Window", Size(300, 300));

		create();
	}
}

void simple() {
	SimpleWin win;
	// win.waitForClose();
	sleep(2 s);
	win.close();
	print("Bye!");
}
