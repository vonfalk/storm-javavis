use core:debug;

Int returnInt(Int v) {
	if (v > 20) {
		return 20;
	}

	10;
}

Str returnStr(Int v) {
	if (v > 20) {
		return v.toS;
	}

	"no";
}

DbgVal returnDbgVal(Int v) {
	if (v > 20) {
		return DbgVal(20);
	}

	DbgVal(10);
}
