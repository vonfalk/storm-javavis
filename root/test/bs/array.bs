use core;
use core:debug;

Int testArray() {
	Array<Dbg> a;

	for (Int i = 0; i < 22; i++) {
		a << Dbg(i);
	}

	Int sum = 0;
	for (Nat i = 0.nat; i < a.count; i++) {
		sum = sum + a[i].get;
	}

	sum;
}
