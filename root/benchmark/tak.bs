use core:debug;

Int tak(Int x, Int y, Int z) {
	if (y < x) {
		tak(
			tak(x - 1, y, z),
			tak(y - 1, z, x),
			tak(z - 1, x, y)
			);
	} else {
		z;
	}
}

// takes 1m28s
void testTak() {
	for (Int i = 0; i < 13; i++) {
		print("Tak " # i);
		print("=> " # tak(i, 0, 0-i));
	}
}
