use core:debug;
use core:lang;
use core:io;

// Evaluate things using the syntax!
Int eval(Str v) {
	Parser<Expr> p;
	p.parse(v, Url());
	if (p.hasError)
		p.throwError;
	var tree = p.tree;
	tree.transform;
}

Expr tree(Str v) {
	Parser<Expr> p;
	p.parse(v, Url());
	p.tree;
}
