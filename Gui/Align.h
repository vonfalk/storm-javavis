#pragma once

namespace gui {

	/**
	 * Horizontal alignment.
	 */
	enum HAlign {
		STORM_NAME(hLeft, left),
		STORM_NAME(hCenter, center),
		STORM_NAME(hRight, right)
	};


	/**
	 * Vertical alignment.
	 */
	enum VAlign {
		STORM_NAME(vTop, top),
		STORM_NAME(vCenter, center),
		STORM_NAME(vBottom, bottom)
	};

}

